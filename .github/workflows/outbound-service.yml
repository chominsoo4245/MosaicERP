name: Build and Test outbound Service

on:
  push:
    paths:
      - 'outbound-service/**'
      - '.github/workflows/outbound-service.yml'

jobs:
  build-outbound:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: outbound-service

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Grant execute permission to gradlew
        run: chmod +x ./gradlew

      - name: Build with Gradle
        run: ./gradlew build